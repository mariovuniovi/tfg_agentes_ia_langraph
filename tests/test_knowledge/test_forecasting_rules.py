# Forecasting validation/exog strategy rules were removed from ml_rules.yaml
# in refactor(forecasting): drop planner-facing validation/exog guidance (now deterministic).
# The three tests that previously asserted on those 6 rule_ids have been deleted.
